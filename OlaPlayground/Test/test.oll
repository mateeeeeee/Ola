Module ID : test.ola

define internal @add__F__F f64(f64 %a,f64 %b) {
entry:
%0 = alloca f64
store f64 %a, ptr %0
%1 = alloca f64
store f64 %b, ptr %1
%2 = alloca f64
%3 = load f64, ptr %0
%4 = load f64, ptr %1
%5 = fdiv f64 %3, %4
store f64 %5, ptr %2
br label exit
return:
br label exit
exit:
%6 = load f64, ptr %2
ret f64 %6
}


define external @main i64() {
entry:
%0 = alloca i64
%1 = s2f i64 28
%2 = s2f i64 4
%3 = call f64 %1, %2, @add__F__F
%4 = alloca f64
store f64 %3, ptr %4
%5 = load f64, ptr %4
%6 = f2s f64 %5
store i64 %6, ptr %0
br label exit
return:
br label exit
exit:
%7 = load i64, ptr %0
ret i64 %7
}

