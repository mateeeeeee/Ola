Module ID : test.ola

declare @Assert void(i8)


declare @AssertMsg void(i8,ptr)


define internal @TestGotoErrorHandling i64() {
entry:
br label if.then
HandleError:
br label exit
return:
br label exit
if.then:
br label HandleError
if.else:
br label exit
if.end:
br label exit
return1:
br label HandleError
goto:
br label exit
exit:
%0 = phi i64 [ -1, HandleError ], [ 42, if.end ]
%1 = phi i64 [ -1, HandleError ], [ 42, if.end ]
ret i64 %1
}


define external @main i64() {
entry:
%0 = call ptr @TestGotoErrorHandling
%1 = icmp eq i64 %0, -1
call i8 %1, @Assert
br label exit
return:
br label exit
exit:
ret i64 0
}

