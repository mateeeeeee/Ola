
noopt bool g() {return true;}
noopt int f() {return 5;}

public int main()
{
	int a = f();
    int b = f() * 2;
    int x = a + b; 
    int result = 0;
    if (g() > 0)
    {
        int y = a + b; 
        result = y;    
    }
    else
    {
        int z = a + b; 
        result = z;    
    }
    
    return result + x; 
}