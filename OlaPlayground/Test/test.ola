import std.file;

public int main()
{
	FileHandle file = FileOpen("abc.txt", "w");
	//bool res = FileIsOpen(file);
	//FileWriteLine(file, "Hello, World!");
    //FileWriteInt(file, 42);
	//FileClose(file);
	return 0;
}