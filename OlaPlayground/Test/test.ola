noinline int factorial_helper(int n, int acc)
{
    if(n <= 1)
    {
        return acc;
    }
    return factorial_helper(n - 1, n * acc);
}

noinline int factorial(int n)
{
    return factorial_helper(n, 1);
}

public int main()
{
    int result = factorial(5);
    return result;
}
