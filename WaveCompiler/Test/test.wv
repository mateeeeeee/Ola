import std.io;
import std.assert;

class S
{
	public void Init(int x) {this.x = x;}
	public ref int X() { return this.x; }
	public int x = 0;
};

public int main()
{
	S s; s.Init(10);
	ref int sx = s.X();
	++sx;
	PrintInt(s.x);
	return 0;
}