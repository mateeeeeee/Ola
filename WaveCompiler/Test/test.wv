import std.assert;
import std.io;

void f(int[] a, int size)
{
    a[0] = 100;
}

public int main()
{
    var a = int[3]{1,2,3};
    a[0] = 10;
    foreach(var e : a)
    {
        PrintInteger(e);
    }
    return a[0];
}