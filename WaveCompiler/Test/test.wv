import std.assert;
import std.io;
import std.string;


public int main()
{
	 char[] name = char[16]{};
    ReadString(name, length(name)); //from std.io
    PrintString(name);

    name[0] = 'L';
    PrintString(name);
}