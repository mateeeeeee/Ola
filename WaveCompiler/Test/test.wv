import std.assert;
import std.io;

 alias IntArray = int[];

public int main()
{
    alias MyInt = int;

    IntArray a = MyInt[6]{1,2,3};
    foreach(MyInt e : a)
    {
        PrintInteger(e);
    }
    return 0;
}