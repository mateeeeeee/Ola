import std.assert;
import std.io;
import std.string;


public int main()
{
	const char[] name = char[16]{};
    ReadString(name, length(name)); //from std.io
    const char[] nameAlias = name;
    PrintString(nameAlias);
}