import std.Assert;

class D
{
	D(int x)
	{
		this.x = x;
	}
	D(int x, int y)
	{
		this.x = x * y;
	}
	public int x = 0;
};

public int main() 
{
   D d1(5);
   D d2(2,3);
   Assert(d1.x == 5);
   Assert(d2.x == 6);

   return 0;
}
