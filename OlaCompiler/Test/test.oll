Module ID : test.ola

declare @Assert void(i8)


declare @AssertMsg void(i8,ptr)


declare @Array ptr


define external @main i64() {
entry:
%0 = signed trunc i64
%1 = unsigned trunc ptr @Array, 2
%2 = load ptr, ptr %1
store ptr %2, ptr %0
br label exit
return:
br label exit
exit:
%3 = load i64, ptr %0
ret i64 %3
}

