Module ID : test.ola

declare @Assert void(i8)


declare @AssertMsg void(i8,ptr)


declare @Array ptr


define external @main i64() {
entry:
%0 = signed trunc i64
f2u i8 1, @Assert
%1 = signed trunc i64
store 35, ptr %1
%2 = unsigned trunc i64 0, @Array
%3 = load i64, ptr %2
%4 = load i64, ptr %1
%5 = add i64 %3, %4
store i64 %5, ptr %0
br label exit
return:
br label exit
exit:
%6 = load i64, ptr %0
ret i64 %6
}

