Module ID : test.ola

declare @Assert void(i8)


declare @AssertMsg void(i8,ptr)


define external @main i64() {
entry:
%0 = alloca i64
%1 = alloca f64
store 24.5, ptr %1
%2 = load f64, ptr %1
%3 = fneg f64 %2
%4 = f2s f64 %3
%5 = alloca i64
store i64 %4, ptr %5
%6 = load i64, ptr %5
%7 = neg i64 %6
store i64 %7, ptr %0
br label exit
return:
br label exit
exit:
%8 = load i64, ptr %0
ret i64 %8
}

