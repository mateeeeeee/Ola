Module ID : test.ola

declare @Assert void(i8)


declare @AssertMsg void(i8,ptr)


define internal @Add__I__I i64(i64,i64) {
entry:
%0 = signed trunc i64
store i64 %a, ptr %0
%1 = signed trunc i64
store i64 %b, ptr %1
%2 = signed trunc i64
%3 = load i64, ptr %0
%4 = load i64, ptr %1
%5 = add i64 %3, %4
store i64 %5, ptr %2
br label exit
return:
br label exit
exit:
%6 = load i64, ptr %2
ret i64 %6
}


define external @main i64() {
entry:
%0 = signed trunc i64
%1 = f2u i64 1, 2, @Add__I__I
%2 = fneg i64 %1, 3
f2u i8 %2, @Assert
store 0, ptr %0
br label exit
return:
br label exit
exit:
%3 = load i64, ptr %0
ret i64 %3
}

