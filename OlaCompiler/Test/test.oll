Module ID : test.ola

declare @Assert void(i8)


declare @AssertMsg void(i8,ptr)


define external @main i64() {
entry:
%0 = alloca i64
%1 = alloca f64
store 7.5, ptr %1
%2 = load f64, ptr %1
%3 = fdiv f64 %2, 3.1
%4 = f2s f64 %3
%5 = alloca i64
store i64 %4, ptr %5
%6 = load i64, ptr %5
store i64 %6, ptr %0
br label exit
return:
br label exit
exit:
%7 = load i64, ptr %0
ret i64 %7
}

