Module ID : test.ola

declare @Assert void(i8)


declare @AssertMsg void(i8,ptr)


declare @Array ptr


define external @main i64() {
entry:
%0 = signed trunc i64
%1 = unsigned trunc ptr @Array, 0, 2
%2 = load i64, ptr %1
store 10, i64 %2
%3 = unsigned trunc ptr @Array, 0, 2
%4 = load i64, ptr %3
store i64 %4, ptr %0
br label exit
return:
br label exit
exit:
%5 = load i64, ptr %0
ret i64 %5
}

