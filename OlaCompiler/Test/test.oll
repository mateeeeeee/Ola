Module ID : test.ola

declare @Assert void(i8)


declare @AssertMsg void(i8,ptr)


define internal @arr ptr [1,2,3,]


define external @main i64() {
entry:
%0 = alloca i64
%1 = gep ptr @arr, 0, 1
%2 = load i64, ptr %1
store i64 %2, ptr %0
br label exit
return:
br label exit
exit:
%3 = load i64, ptr %0
ret i64 %3
}

