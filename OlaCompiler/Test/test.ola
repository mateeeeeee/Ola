import std.assert;

class Base 
{
    int x = 1;

    public int GetX() virtual const {return x;}
};

class Derived extends Base
{
    int x = 2;
    public int GetX() virtual const {return x;}
};

public int main() 
{
    Derived d;
    ref Base b = d;
    return b.GetX();
    return 0;
}
