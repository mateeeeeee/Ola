import std.Assert;

class D
{
	D(int x)
	{
		this.x = x;
	}
	D(int x, int y)
	{
		this.x = x * y;
	}
	public int X() const {return x;}
	private int x = 0;
};

public int main() 
{
   D d(5);
   D d2(2,3);
   return d2.X();
}
