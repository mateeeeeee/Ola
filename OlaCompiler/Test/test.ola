import std.assert;

private void modifyArray(int[] arr)
{
	arr[0] = 100;
}

private void modifyArray2(int[3] arr)
{
	arr[0] = 100;
}

public int main()
{
	int[] arr = {1,2,3};
	modifyArray2(arr);
	Assert(arr[0] == 1);
	modifyArray(arr);
	Assert(arr[0] == 100);
	return 0;
}