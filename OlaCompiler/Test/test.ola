import std.Assert;

class Base 
{
    int x = 1;
};

class Derived extends Base
{
    int x = 2;
    public int GetSum() const { return this.x + super.x; }
};



public int main() 
{
    Derived d;
    return d.GetSum();
}
