import std.assert;

void TestWhileLoopWithBreak()
{
    int sum = 0;
    int i = 1;

    while (true)
    {
        sum += i;
        i++;

        if (i > 5)
        {
            break;
        }
    }
    Assert(sum == 15);
}


public int main()
{
    TestWhileLoopWithBreak();
    return 0;
}