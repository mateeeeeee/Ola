import std.assert;


public void main()
{
	//todo
	//cover both local and global declarations, using var, int[], initializer list with and without e.g. int[5]{}
	//cover bracket operator on both global and local, check that it throws an compiler error when accessing array with constant expression outside bounds
	//cover sizeof operator
	//cover length operator
	//cover passing arrays to functions
}